# Fail2Ban filter for Nginx Proxy Manager admin interface
[Definition]
failregex = ^<HOST> -.*POST /api/tokens.*HTTP/1\.." (401|403)
            ^<HOST> -.*POST /api/auth.*HTTP/1\.." (401|403)
            ^<HOST> -.*"(GET|POST) /api/.*HTTP/1\.." (401|403|404|500)
ignoreregex =

[Init]
maxlines = 10